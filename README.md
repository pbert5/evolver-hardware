# evolver-hardware

Exclusive owner of serial discovery, framing, readout, provisioning plumbing,
and bounded local IPC. Tests use fakes/simulators; no physical hardware is
actuated and there is no database or BAL catalog dependency.

Origin: Meta WebUI `wire-in-cli` `652cc5d`.
